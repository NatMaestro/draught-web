# Optional images (from `draught-fe/assets`)

Copy into `public/` for parity with the Expo app:

- `splash-icon.png` — splash logo
- `Game-Logo.png` — home header logo
- `play-a-game-logo.png` — play screen illustration

The app degrades gracefully if files are missing (fallbacks / hidden images).
