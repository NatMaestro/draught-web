# Local development — Vite proxies /api and /ws → http://localhost:8000 (see vite.config.ts)
VITE_API_BASE_URL=/api
# Real-time moves + chat (set to false to use REST-only moves)
VITE_USE_GAME_WS=false
